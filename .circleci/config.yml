version: 2.1

# orbs:
#   node: circleci/node@4.7.0

jobs:
  build:
    docker:
      - image: 'circleci/node@4.7.0'
    steps:
      - checkout
      - run: npm run build

workflows:
  build:
    jobs:
      - build
